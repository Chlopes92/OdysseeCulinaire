import React, { Component } from 'react'; 
import styles from './SelectedProduct.module.css';

export class SelectedProduct extends Component {
  static propTypes = {}

  render() {
    return (
      <div className={styles.rond}>SelectedProduct</div> 
    );
  }
}

export default SelectedProduct;
